{
  "name": "tf-snag-tab",
  "version": "0.1.0",
  "private": true,
  "description": "Azure DevOps build-results tab that renders the tf-snag drift report.",
  "scripts": {
    "logo": "go run ./tools/genlogo/main.go",
    "stamp": "node tools/stamp-tasks.js",
    "tab:dev": "node tools/tabserver.js",
    "package": "tfx extension create --manifest-globs vss-extension.json --output-path dist",
    "package:dev": "tfx extension create --manifest-globs vss-extension.json --overrides-file configs/dev.json --output-path dist",
    "publish": "tfx extension publish --manifest-globs vss-extension.json",
    "publish:dev": "tfx extension publish --manifest-globs vss-extension.json --overrides-file configs/dev.json"
  },
  "_comment_tfx": "tfx-cli is PINNED, not ranged. 0.22+ moves azure-devops-node-api from ^14 to ^17, and on 2026-09-16 every `tfx extension show` against the Marketplace hung until the publish workflow's 60s cap - four attempts, no output, twice in a row. 0.21.3 is the last version that published (dev v0.6.29, 2026-09-15). Re-test a bump against a real publish before taking it.",
  "devDependencies": {
    "tfx-cli": "0.21.3",
    "vss-web-extension-sdk": "^5.141.0"
  }
}
